/** Stable model-facing operating discipline for the Team tool family. */
export const AGENT_SWARM_USAGE_PROMPT = `Use agent_swarm_* for a requested coordinated Team, within your current role and available tools. The main/root Agent enters through agent_swarm_create_managed with the user's complete objective verbatim, remains outside the Team, then ends its turn (at most one managed-Team list; no polling). Team participants follow their role persona; agent_swarm_create is the existing-Captain compatibility entry.
Team state comes from the tools, not workspace files. Use current revisions and exact attempt ids; stop stale attempts. Write scopes are coordination hints, not filesystem authorization. Queued mail is durable: never resend it; quiet waits for the recipient's next turn, wakeup resumes it. List tools paginate; jobs are read-only task projections.`
