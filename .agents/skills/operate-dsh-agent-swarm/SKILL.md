---
name: operate-dsh-agent-swarm
description: "Configure and operate dsh-agent-swarm Teams: create teams and members, define and supervise tasks, use mailbox and memory, inspect live progress, diagnose stalls, recover fenced work, and close teams safely. Use dsh-plugin-development instead for source changes."
---

# Operate dsh-agent-swarm

Read [references/operation-guide.md](references/operation-guide.md) before creating or supervising a Team. It defines the tool sequence, fencing rules, long-running supervision loop, recovery boundaries, and live read authorities.

Use the graph only to locate a static contract or implementation anchor:

```sh
node .agents/skills/operate-dsh-agent-swarm/scripts/query-graph.mjs search <text>
```

The helper verifies generated bytes before every query and fails closed. Read [references/graph-navigation.md](references/graph-navigation.md) for its commands and interpretation rules. The graph is a static contract index: mechanical records prove only source location, and implementation, verification, acceptance, and availability are four independent axes.

Use `agent_swarm_status`, `agent_swarm_list_tasks`, `agent_swarm_list_jobs`, and authorized memory reads for current state; never substitute graph or prose for runtime authority. For any source implementation, refactor, code-level debugging, schema change, plugin validation, or release task, stop this operating workflow and load `$dsh-plugin-development`.
