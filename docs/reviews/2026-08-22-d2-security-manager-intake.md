# D2 安全审查 — Manager Intake（2026-08-22）

- Intake 人：项目管理工程师（ZCode 会话）
- 对象：`docs/reviews/2026-08-22-d2-security-review.md`（verdict **CONDITIONAL**）
- 结论采信：五威胁面全覆盖（含委派书点名的"第三形态"——审查员真找到了：F4 跳过验收的静默晋升路径）；发现分级与证据链（file:line）完备；与 P0-P7 声明的交叉核验独立且诚实（"实证成立但不得外推为结构性保证"的措辞正是正确姿态）。**verdict 采信。**

## 逐项裁决与排期

| 发现 | 裁决 | 处置 |
|---|---|---|
| F1（P1）候选代码以 PM 主体执行 | **接受**。ADR-0008 安全边界的主体层缺位是真实缺口——tarball 安装有 `--ignore-scripts` 而仓库级 install 没有，防护不对称的实证一针见血 | D2 全面开放硬前置：降权执行（deny-write ACL 覆盖 dogfood root 与 ~/.dsh + 低权 token）——立案 |
| F2（P1）验收域 env 继承暴露凭据 | **接受**。"只收工件不收凭据"在实现层只守住了一半 | 与 F1 同案：env 白名单先行（成本低，独立小改） |
| F3（P2）promote 半应用态无补偿 | **接受**。status 不对账已装字节 digest 是机检盲区 | 阻断清单：补偿重装 + status 对账 + 分歧修复子命令 |
| F4（P2）verdict 校验弱于自证 | **接受**。P4b 的注入手段同时就是缺口实证——演练设计的意外红利 | 阻断清单：强制八 gate 三重在场 + evidenceBaseDir + verdictRef 交叉 |
| F5（P2）台账无外部锚 | **接受**。OQ-11 "不打 tag" 裁决被审查翻案有理 | 阻断清单：链尾锚 git tag（与 F2 联动防密钥入 env） |
| F6-F10（P3） | 接受为低影响 | 随上述修复顺带处理（F6 的 fail-safe 缺口 + F9 的 marker 校验入下次触碰） |

## D2 开放状态更新

**维持受控开放**（与 M3 出口宣言一致，审查将其清单化）：受控回路（PM 盯守 + 单写者 + 候选来源为本团队）下风险敞口可接受。**全面并行编码开放的前置**：F1/F2 修复（降权 + env 白名单）+ F3/F4/F5 修复——合并为单一 issue"控制面安全加固第一迭代"。

## 前置条件 B 裁决（PM，依审查输入）

采纳审查建议 2 的三先行项路径（不等完整 D4）：降权执行 + env 白名单 + 链尾锚 git。三项落地后 F1/F2/F5 降级，D2 可全面开放；否则 D4 沙箱提前为硬前置。声明级围栏的定位修正为"良性防误写机制"（对抗恶意候选不够）——docs/13 §3 表同步。

## 流程备注

审查席位按 docs/12 无限制运行（332 秒完成，单轮）；报告逐字转录未改写；本 intake 仅处置排期。
